export const ROUTES = {
    HOME: { path: 'home', redirect: '/home' },
    LIVES: { path: 'lives', redirect: '/lives' },
    LOGIN: { path: '', redirect: '/' },
    ACCOUNT: { path: 'account', redirect: '/account' },
    LOADCONTENT: { path: 'load-content', redirect: '/load-content' },
    URLBASE: { path: '', redirect: '/' },
}